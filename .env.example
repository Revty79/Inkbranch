# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Auth scaffolding (to be wired in next prompt)
AUTH_URL=http://localhost:3000
AUTH_SECRET=replace-with-a-long-random-string
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace-with-a-long-random-string

# Demo credentials are currently defined in src/features/auth/users.ts
# admin@inkbranch.local / inkbranch-admin
# reader@inkbranch.local / inkbranch-reader

# Database connection used by Drizzle migrations/schema
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/inkbranch
